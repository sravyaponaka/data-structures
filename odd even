#include<stdio.h>
int main()
{
	int n,i;
	printf("enter the number:");
	scanf("%d",&n);
	int arr[n];
	for (i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
		if(arr[i]%2==0)
		{
			printf("\neven-%d",arr[i]);
		}
		else
		{
			printf("\nodd-%d",arr[i]);
		}
	}
}
